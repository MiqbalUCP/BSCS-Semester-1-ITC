#include<iostream>
using namespace std;
int main()
{
	int length, width;
	cout << "enter the length and width of rectangle" << endl;
	cout << " enter the lenth " << endl;
	cin >> length;
	cout << " enter the width " << endl;
	cin >> width;
	cout << "the area of rectangle is " << length * width << endl;
	cout << "the parameter of rectangle is " << length + length + width + width << endl;
	return 0;


}
